# Minimal dependencies: every CV / ML algorithm is implemented from scratch.
numpy>=1.23.0       # Numeric arrays and vectorized operations
Pillow>=9.0.0       # File I/O only

# Optional: debugging visualization helpers
matplotlib>=3.5.0

# Testing
pytest>=7.0.0
